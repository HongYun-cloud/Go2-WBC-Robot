让 WBC 姿态 PD 真正获得力通道自由度（基于 PlotJuggler 证据：tau_pitch_mpc/wbc 完全重合 → WBC 力死跟 f_mpc，PD 被架空）：

1. config/mpc.yaml 权重调整：
   - FI: 100 → 10（全部 12 维）：把 f_mpc 从"硬目标"降级为"前馈参考"，允许 WBC 的 f 偏离它。
   - C 动力学行（后 18 维）：10 → 100（摆动腿跟踪前 12 维保持 800 不动）：逼 QP 让 f 与 a 保持动力学一致，使姿态 PD 的 a 指令能传导到接触力。

2. src/ros2/go2_control_node.cpp computePoseAccDes 增益恢复（坐标系 bug 已修，撤掉人工补偿）：
   - Kp_ang: (-3, 3, 3) → (50, 50, 30)（roll 负号翻回正，量级从 ±3 恢复到有作用的量级）
   - Kd_ang: (-0.5, 0.5, 0.5) → (10, 10, 5)
   - 线通道增益不动。

3. 重新编译 go2_robot（yaml 是运行时读取，但增益是代码需要编译；yaml 改动同时生效于 install 目录由编译拷贝）。

4. 验证（PlotJuggler，重启节点与 PlotJuggler）：
   - 预期 tau_pitch_wbc / tau_roll_wbc 开始偏离 tau_*_mpc，且偏离方向与 q_des_ang_y/x 相关 → PD 已插手力分配。
   - 观察 f_cone_* 是否频繁 >1（FI 降级后 QP 更容易把切向力推出摩擦锥，若打滑明显则下一步给 WBC 加摩擦锥约束）。
   - 观察 roll/pitch 偏差幅值与振荡情况，Kp_ang 可在此基础上再调。