补齐 Bezier_Math::evaluatePos 缺失的四阶贝塞尔分支（仅改 src/Planner/SwingLegPlanner.cpp:220-223）

现状：lift_off 处 z_up/z_down 各有 3 个中间点（mid.size()==3 → 四阶贝塞尔，5 个控制点 [S, M0, M1, M2, E]），而 evaluatePos 的四阶分支只有 "// 待续" 注释，函数返回未初始化的局部变量 p（UB）→ 摆动期 p_z 是垃圾值（你观察到摆动相 z≈0、支撑相呈方波保持）。

修改：在 evaluatePos 的 degree == 3 分支实现四阶 Bernstein 基公式（与已有的 evaluateVel/evaluateAcc 四阶公式系数一致）：
  p = (1-t)⁴·S + 4t(1-t)³·M0 + 6t²(1-t)²·M1 + 4t³(1-t)·M2 + t⁴·E

不改控制点本身，完全按你现有的 z_up/z_down 设计（[0.023, 0.023, Walk_H] / [Walk_H, 0.023, 0.023]）生效。

验证：
- 重新编译 go2_robot。
- 跑仿真，PlotJuggler 确认：contact=0（摆动相）p_ref_z 不再恒为 0，而是从地面抬升至 Walk_H 再落下；contact=1（支撑相）保持地面高度平稳。