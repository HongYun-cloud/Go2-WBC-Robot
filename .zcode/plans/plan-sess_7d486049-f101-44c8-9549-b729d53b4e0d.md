我将按照方案A创建完整的12关节链结构来修改MuJoCo模型：

## 详细实施计划

### 1. 添加关节类定义
- 在defaults部分添加新的`thigh`关节类定义
- 现有的`hip`类专门用于髋关节，`thigh`类用于大腿关节，`knee`类用于小腿关节

### 2. 重构身体层次结构
- 将每个腿部分为3个关节：髋关节→大腿关节→小腿关节
- 左前腿：FL_hip → FL_thigh → FL_calf
- 右前腿：FR_hip → FR_thigh → FR_calf  
- 左后腿：RL_hip → RL_thigh → RL_calf
- 右后腿：RR_hip → RR_thigh → RR_calf

### 3. 更新执行器配置
- 为每个髋关节添加独立的执行器
- 将现有执行器重新映射到对应的大腿关节
- 总共12个执行器：FL_hip, FL_thigh, FL_calf, FR_hip, FR_thigh, FR_calf, RL_hip, RL_thigh, RL_calf, RR_hip, RR_thigh, RR_calf

### 4. 更新传感器配置
- 添加髋关节位置和速度传感器
- 保持现有传感器兼容性

### 5. 更新关键帧配置
- 调整所有关键帧以包含12个关节角度
- 确保初始姿态和运动姿态的正确性

### 6. 验证配置
- 检查关节顺序与Pinocchio模型的一致性
- 确保控制算法能正确处理12维输入

这个计划将创建真正的12自由度物理模拟，与现有的12维控制架构完全匹配。