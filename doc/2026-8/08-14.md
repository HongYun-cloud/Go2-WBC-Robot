## 2026-08-14 工作日志

## 0. 闲话
草了，前几天考了个驾照去，还好科2过了，妈的这几天是一点没写啊，写到轨迹点和规划了吧接着往下写吧

上次好像写到轨迹点生成了，每个running生成一个瞬时点
## 1. 今日目标

## 2. 今日问题

## 3. 解决方案

## 4. 今日结果
写了贝塞尔曲线生成pos,vel,acc，明天看看怎么加入到mpc那一套里

## 5. 今日思路
之前写轨迹生成是生成的一整个轨迹线，导致点特别多，现在改成实时生成，按照计算相位归一化后的时间t来生成瞬时点，因为目前这个线程是高频的，所以计算点很快，应该不会存在计算的时间太长的问题  

明天要把for里的贝塞尔控制关键点改一下，不能一直更新，根据公式，后两个点时间权重是t^2和（1-t）* t^2 当时间小的时候权重占比较小，所以可以更新end点和mid_end点，后期就锁住不更新，然后前两个点一直锁住，明天吧这个写一下 ，这个叫做**动态重规划**(新名词嘿) 