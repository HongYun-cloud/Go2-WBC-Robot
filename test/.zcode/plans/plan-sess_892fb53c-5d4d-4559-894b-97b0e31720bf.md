修改 /home/hong/Movement_Control/Go2Robot/test/bezier_watch.py：

1. 抽取通用函数 `calc_bezier_derivatives(P, s, T)`：用 Bernstein 基通用公式计算任意阶贝塞尔的位置、速度（÷T）、加速度（÷T²），三阶和五阶共用。

2. 新增五阶贝塞尔摆动腿轨迹（6 个控制点，垂直起降策略）：
   - P0=(0,0), P1=(0,h), P2=(0.25,h), P3=(0.75,h), P4=(1,h), P5=(1,0)
   - 摆动高度 h=0.2，步长 1.0，摆动周期 T=1s，与三阶一致

3. 绘图改为 3×2 子图布局：左列三阶、右列五阶对比；三行依次为足端空间轨迹（X-Z）、速度曲线、加速度曲线。保留原有三阶数据与样式，五阶用相同配色以便对比。

4. 运行脚本验证图像正常生成。